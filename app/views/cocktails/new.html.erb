<h2>Add a Cocktail</h2>

<hr>
<%= simple_form_for @cocktail do |r| %>

  <%= r.input :name, label: 'Enter un nom qui déchire',  placeholder: 'Bloody Mary' %>

  <%= r.input :photo, as: :attachinary %>
  <%= r.button :submit, class: 'btn-success btn-block' %>
<% end %>
<hr>
